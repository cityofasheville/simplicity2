/*
 * SearchBox Messages
 *
 * This contains all the text for the SearchBox component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.SearchBox.header',
    defaultMessage: 'Search!',
  },
});
