version: 0.2

phases:
  install:
    runtime-verions:
      nodejs: 8
    commands:
      - echo "install step"
      - npm install
  build:
    commands:
      - npm run build
      - npm run devDeploy
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id "${DISTRIBUTION_ID}" --paths '/*'