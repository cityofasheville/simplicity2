version: 0.2

phases:
  install:
    commands:
      - echo "install step"
      - npm install
  build:
    commands:
      - npm run build
      - npm run devDeploy
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id "${DISTRIBUTION_ID}" --paths '/*'