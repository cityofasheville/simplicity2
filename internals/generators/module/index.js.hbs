/*
 *
 * {{properCase name }}
 *
 */

{{#if wantMessages}}
import { FormattedMessage } from 'react-intl';
import messages from './{{ camelCase name }}Messages';
{{/if}}

{{#if wantActionsAndReducer}}
import { defaultAction } from './{{ camelCase name }}Actions';
{{/if}}

export class {{ properCase name }} {
  constructor(store) {
    this.store = store;
    store.subscribe(this.storeListener.bind(this));
  }

  storeListener() {
    const state = this.store.getState();
  }
}
