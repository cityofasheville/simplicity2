import React from 'react';

const Locations = () => (
  <div className="col-xs-12">
    <h1>Locations</h1>
    <h2>Map of Locations: way to search for locations and use map to find locations </h2>
    <h3>Locations on the map should clickable with a pop-up or panel that
    gives some basic details and show a link to the location page</h3>
  </div>
);

Locations.propTypes = {};

export default Locations;
